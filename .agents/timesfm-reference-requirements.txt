# Pinned environment for generate-timesfm-reference.py.
# The TimesFM package itself is imported from the pinned source checkout.
numpy==1.26.4
torch==2.2.2
huggingface_hub==0.36.0
safetensors==0.5.3
